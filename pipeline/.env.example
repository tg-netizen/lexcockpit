# Supabase (LexCockpit project)
SUPABASE_URL=https://fstoenrocfyzdsgmiknj.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Optional: call the deployed Edge Function instead of in-process logic
INGEST_EDGE_URL=https://fstoenrocfyzdsgmiknj.supabase.co/functions/v1/ingest-news
CRON_SECRET=replace-me

# LLM — set one provider
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-3-5-haiku-latest

# Mirror drafts into the LexDigestGlobal content repo (so Cockpit sees them)
GITHUB_PAT=ghp_...
GITHUB_REPO=tg-netizen/lexdigestglobal-real-version
GITHUB_BRANCH=main
GITHUB_CONTENT_PATH=content/articles/
