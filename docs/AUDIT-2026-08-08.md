# LexCockpit audit — 2026-08-08

Environment note: this cloud agent runs on Linux and **cannot launch the SwiftUI
macOS app on your Mac**. Verification on-device: pull the branch in GitHub Desktop,
`swift run LexCockpit` or open the release build, then follow the checklist below.

## Findings addressed in this PR

| Severity | Issue | Fix |
|----------|-------|-----|
| High | Free scan-only waiting list invisible in Mac app | Supabase client + Overview **News waiting list** |
| High | Anon key blocked by RLS | `grant select on review_queue to anon` migration |
| Med | AI drafts labeled CONCEPT | Pill = `AI DRAFT`; `isAIDraft` = `ai_generated` only |
| Med | Draft / AI stats double-count | In-draft excludes AI drafts |
| Med | No Settings for Supabase | Settings → **Ingest** (URL + anon key + Test) |
| Med | Content library no review filter | **Review** toggle + waiting-list banner |
| Low | Hub ignored ingest work | Today rail shows waiting-list count |

## Still open / later

- Reject / promote actions from the Mac app (needs write policies)
- Auto-refresh timer for content + queue while Overview is open
- Sidebar name collision: Topics → Pipeline ≠ news ingest
- README Xcode drag-in list still under-documents the full SPM target

## On-Mac checklist

1. GitHub Desktop → fetch `cursor/content-ingestion-pipeline-1e36` (or merge PR)
2. Run SQL grants from `supabase/SCAN_ONLY_SETUP.md` (incl. `anon` select)
3. Deploy / trigger `ingest-news` once (scan_only)
4. LexCockpit → Settings → Ingest → paste URL + **anon** key → Test
5. Overview → **News waiting list** should list queued items
